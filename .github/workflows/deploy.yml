name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "namibia-expedition-live.html"

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Copy to index.html
        run: cp namibia-expedition-live.html index.html

      - name: Inject Supabase credentials
        run: |
          sed -i "s|PASTE_PROJECT_URL|${{ secrets.SUPABASE_URL }}|g" index.html
          sed -i "s|PASTE_ANON_PUBLIC_KEY|${{ secrets.SUPABASE_ANON_KEY }}|g" index.html

      - name: Stamp version and publish time
        run: |
          sed -i "s|__APP_VER__|v${{ github.run_number }}.$(git rev-parse --short HEAD)|g" index.html
          sed -i "s|__APP_BUILT__|$(date -u +%Y-%m-%dT%H:%M:%SZ)|g" index.html

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          include_files: index.html
